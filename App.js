import { createAppContainer } from 'react-navigation';
import { AppDrawerNavigator } from 'app/components/AppDrawerNavigator';

const App = createAppContainer(AppDrawerNavigator);
export default App;
